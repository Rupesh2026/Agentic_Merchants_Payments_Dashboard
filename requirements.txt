# Dashboard runtime deps — Streamlit Cloud
# AI Analyst page requires google-adk locally; other 5 pages work without it.

# ── Core data / DB ────────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
pgvector==0.3.2
python-dotenv==1.0.1

# ── OpenAI (RAG retriever embeddings) ─────────────────────────────────────────
openai==2.41.1

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit==1.35.0
plotly==5.22.0
streamlit-extras==0.4.2
